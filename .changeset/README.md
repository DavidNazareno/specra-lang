# Changesets

Specra uses Changesets to manage package versions and release notes.

Create a release note with:

```bash
pnpm changeset
```

Then apply version updates with:

```bash
pnpm version-packages
```
